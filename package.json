{
  "name": "social-network-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.2"
  },
  "scripts": {
    "ios": "lerna run ios --stream --parallel",
    "android": "lerna run android --stream --parallel"
  }
}
